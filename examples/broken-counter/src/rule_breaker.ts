/**
 * Baseline module for the CodeClaw **review** demo. Keep this file clean in git.
 * Run `scripts/smear-for-review.sh` to append a small diff that violates `.codeclaw/rules.md`.
 */
export function reviewDemoPlaceholder(): number {
  return 0
}
